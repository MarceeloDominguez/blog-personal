---
interface Props {
  link: string;
}

const { link } = Astro.props;
---

<a
  href={link}
  target="_blank"
  rel="noopener noreferrer"
  class="inline-flex gap-1 font-bold py-2 px-4 rounded border border-gray-900 hover:border-gray-500 transition duration-300 ease-in-out"
>
  <slot name="before" />
  <slot>Button</slot>
  <slot name="after" />
</a>
